TODO list

4) Exception error handling, maybe recovery when possible
5) Make nested styles for sections
6) Fill different output fields (publisher, isbn, etc.)

DONE:

-Back references
-Translit support (compatible with Roman's converter Translit.xml configuration file)
-Switch from libiconv to tiniconv
    - (we can use one of three converter implementations now - libiconv, ICU4C, tiniconv)
-Linux/unix makefile added
